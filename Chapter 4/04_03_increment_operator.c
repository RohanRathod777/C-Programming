#include <stdio.h>

int main()
{
    int i = 5;
    // printf("the value of i++ is %d\n", i++);---> Post-increment
    // i++---> Pahale print fir increment

    // printf("the value of ++i is %d\n", ++i);---> Pre-increment
    //++i---> Pahale increment fit print

    // i+=10 ---> Increment i by 10
    
    printf("the value of i++ is %d\n", i++);
    printf("the value of i is %d\n", i);
    return 0;
}