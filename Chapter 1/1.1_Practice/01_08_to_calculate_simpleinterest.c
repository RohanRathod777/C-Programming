#include <stdio.h>

int main()
{
    int principle = 100, rate = 4, year = 1;
    int simpleinteres = (principle * rate * year) / 100;
    printf("The value of simpeleinterest is %d", simpleinteres);

    return 0;
}