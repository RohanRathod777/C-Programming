#include <stdio.h>

int main()
{
    int a = 2, b = 2;

    printf("sum of a +b is %d\n", a + b);
    printf("sum of a -b is %d\n", a - b);
    printf("sum of a *b is %d\n", a * b);
    printf("sum of a /b is %d\n", a / b);
    return 0;
}